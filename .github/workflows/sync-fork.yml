name: Sync fork
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Sync from upstream
        run: |
          git remote add upstream https://github.com/shareAI-lab/share-best-mcp.git
          git fetch upstream
          git reset --hard upstream/main
          git push origin main --force
